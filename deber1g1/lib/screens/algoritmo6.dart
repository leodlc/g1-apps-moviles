import 'package:flutter/material.dart';

class SixthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Tv Room')),
      body: Center(child: Text('Tv Room Screen')),
    );
  }
}
