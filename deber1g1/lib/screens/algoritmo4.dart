import 'package:flutter/material.dart';

class FourthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Bathroom')),
      body: Center(child: Text('Bathroom Screen')),
    );
  }
}
